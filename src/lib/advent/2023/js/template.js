import * as txt from '$lib/advent/2023/inputs/<day>.txt?raw';

export function task() {
    let lines = txt.default.split('\n')

    for (let line of lines) {
        // skip empty lines
        if(line == '') continue

        // code goes here
    }

    return [0, 0]
}